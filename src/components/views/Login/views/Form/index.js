import React from "react";

const Form = (props) => {
  const {
    onHandleChange,
    onHandleSubmit,
    data: { email, password, emailError, passwordError },
    onHandleBlur,
  } = props;
  return (
  );
};

export default Form;
